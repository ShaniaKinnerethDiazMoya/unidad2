#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "";
    if (strlen(str) == 0) {
        printf("La cadena está vacía.\n");
    } else {
        printf("La longitud de la cadena es: %lu\n", strlen(str));
    }
    return 0;
}
