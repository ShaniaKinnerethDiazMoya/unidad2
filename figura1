#include <stdio.h>

int main() {
    int lado = 5;
    for (int i = 0; i < lado; i++) {
        for (int j = 0; j < lado; j++) {
            printf("* ");
        }
        printf("\n");
    }
    return 0;
}
