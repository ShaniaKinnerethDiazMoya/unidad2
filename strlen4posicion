#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello";
    size_t len = strlen(str);
    for (size_t i = 0; i < len; i++) {
        printf("Carácter en la posición %zu: %c\n", i, str[i]);
    }
    return 0;
}
